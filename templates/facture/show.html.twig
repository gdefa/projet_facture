{% extends 'base.html.twig' %}

{% block title %}Facture{% endblock %}

{% block body %}
    <h1>Facture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ facture.id }}</td>
            </tr>
            <tr>
                <th>Utilisateur</th>
                <td>{{ facture.utilisateur }}</td>
            </tr>
            <tr>
                <th>DateFacture</th>
                <td>{{ facture.DateFacture }}</td>
            </tr>
            <tr>
                <th>NombreJoursTravail</th>
                <td>{{ facture.NombreJoursTravail }}</td>
            </tr>
            <tr>
                <th>PriceHours</th>
                <td>{{ facture.PriceHours }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_facture_index') }}">back to list</a>

    <a href="{{ path('app_facture_edit', {'id': facture.id}) }}">edit</a>

    {{ include('facture/_delete_form.html.twig') }}
{% endblock %}
